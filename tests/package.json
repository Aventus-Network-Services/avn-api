{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "queryTests": "mocha --timeout 20000000 \"queryTests.js\" --gateway",
    "queryTests:dev": "mocha --timeout 20000000 \"queryTests.js\" -- --gateway dev",
    "test": "npm run queryTests",
    "pretty": "prettier --config ./.prettierrc.json --check \"**/*.{js,json}\"",
    "pretty:fix": "prettier --config ./.prettierrc.json --write \"**/*.{js,json}\"",
    "format": "npm run pretty:fix"
  },
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "avn-api": "^5.7.0",
    "chai": "^4.5.0"
  },
  "devDependencies": {
    "mocha": "^11.7.5"
  }
}
